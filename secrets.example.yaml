# Скопируйте в secrets.yaml и подставьте свои значения. secrets.yaml — в .gitignore.
wifi_ssid: "ВАША_ДОМАШНЯЯ_СЕТЬ"
wifi_password: "ПАРОЛЬ_ДОМАШНЕЙ_СЕТИ"

# ESPHome native API — сгенерируйте свой ключ: https://esphome.io/components/api.html
# (base64, 32 байта). Пример генерации: python3 -c "import base64,os;print(base64.b64encode(os.urandom(32)).decode())"
api_encryption_key: "СГЕНЕРИРУЙТЕ_СВОЙ_КЛЮЧ_BASE64"

# Пароль резервной точки доступа ESPHome (fallback hotspot)
ap_fallback_password: "ПРИДУМАЙТЕ_ПАРОЛЬ"
